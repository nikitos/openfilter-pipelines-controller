# Production values for openfilter-pipelines-controller
# This is a sample configuration for production deployments

# Inject OTel exporter config into every filter container the controller spawns
# so spans/metrics flow to the in-cluster otel-collector (and from there to
# Cloud Trace via the googlecloud exporter).
#
# Use "otlp" (not "otlp_grpc") here: openfilter's metrics factory
# (observability/client.py) only matches "otlp" and silently falls through to
# ConsoleMetricExporter on any other value, while the tracing factory
# (observability/tracing.py) accepts both — so "otlp" is the only string that
# wires gRPC for both signals against today's openfilter.
telemetry:
  exporterType: "otlp"
  exporterOtlpEndpoint: "otel-collector.monitoring.svc.cluster.local:4317"

replicaCount: 1

image:
  repository: us-west1-docker.pkg.dev/plainsightai-prod/oci/openfilter-pipelines-controller
  pullPolicy: IfNotPresent
  tag: ""

# Production resource limits
resources:
  limits:
    cpu: 1000m
    memory: 512Mi
  requests:
    cpu: 100m
    memory: 256Mi

# Enhanced security context
podSecurityContext:
  runAsNonRoot: true
  runAsUser: 65532
  runAsGroup: 65532
  fsGroup: 65532
  seccompProfile:
    type: RuntimeDefault

securityContext:
  allowPrivilegeEscalation: false
  readOnlyRootFilesystem: true
  runAsNonRoot: true
  runAsUser: 65532
  capabilities:
    drop:
    - ALL

# Enable Valkey for production workloads
valkey:
  enabled: true
  architecture: replication
  auth:
    enabled: true
  primary:
    persistence:
      enabled: true
      size: 20Gi
      storageClass: ""  # Use default storage class
    resources:
      limits:
        cpu: 500m
        memory: 512Mi
      requests:
        cpu: 250m
        memory: 256Mi
  replica:
    replicaCount: 2
    persistence:
      enabled: true
      size: 20Gi
      storageClass: ""  # Use default storage class
    resources:
      limits:
        cpu: 500m
        memory: 512Mi
      requests:
        cpu: 250m
        memory: 256Mi

# Production affinity rules for high availability
affinity:
  podAntiAffinity:
    preferredDuringSchedulingIgnoredDuringExecution:
      - weight: 100
        podAffinityTerm:
          labelSelector:
            matchExpressions:
              - key: app.kubernetes.io/name
                operator: In
                values:
                  - openfilter-pipelines-controller
          topologyKey: kubernetes.io/hostname
