apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "openfilter-pipelines-controller.fullname" . }}-test-connection"
  labels:
    {{- include "openfilter-pipelines-controller.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "openfilter-pipelines-controller.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
